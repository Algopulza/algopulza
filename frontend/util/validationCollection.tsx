export const checkSpace = (item: string) => {
  if (item === '') {
    return false
  } else {
    return true
  }
}

export const checkPassword = (password: string) => {
  if (password === '') {
    return false
  } else {
    return true
  }
}