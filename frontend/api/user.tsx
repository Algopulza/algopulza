import { fileApi } from "./api";

// export const profileImage = async (formData:any) => {
//   const response = await fileApi.patch(`/api/v1/members/profileImage`, formData);
//   return response.data;
// };