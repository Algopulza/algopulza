import styled from "styled-components"

const Container = styled.section`
  padding: 5vw;
`

const Category = () => {
  return (
    <Container>
      category 화면 영역입니다.
    </Container>
  )
}

export default Category
